[project]
name = "babel"
version = "0.1.0"
description = "This small app serves to manage the records of my own library."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas==2.3.1",
    "rich==14.1.0",
    "streamlit==1.48.1",
]
